abs:
* support multiple arches, or find a way to roll 'any' into current arch

makeworld:
* Somehow parse options once for correctness (and not copy paste from makepkg)
* tee build output to screen and to log file (or use makepkg's built in logging)

svn2abs:
